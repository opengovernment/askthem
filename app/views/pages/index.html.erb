<section>
  <ul>
    <% @jurisdictions.each do |jurisdiction| %>
      <li>
        <%= link_to jurisdiction['name'], jurisdiction_path(jurisdiction: jurisdiction.id) %>
      </li>
    <% end %>
  </ul>
</section>
