<section class="related_focus">
  <ol class="bills">
    <%= render @bills %>
  </ol>

  <%= paginate @bills, outer_window: 1, params: {format: nil}, replace: '.related_focus', scroll: '.related_wrap' %>
</section>
