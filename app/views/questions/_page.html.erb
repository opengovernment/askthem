<div id="page">
  <ol class="questions">
    <%= render @questions %>
  </ol>

  <% if @questions.any? -%>
    <%= paginate @questions, outer_window: 1, params: {format: nil}, replace: '#page', scroll: 'section header' %>
  <% end -%>
</div>