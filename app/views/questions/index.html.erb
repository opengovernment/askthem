<%= render 'shared/navigation' %>

<section class="questions listings">
  <header>
    <h1>Questions</h1>
    <%= link_to 'Ask a Question', new_question_path(@jurisdiction.abbreviation), class: 'cta_pill' %>
  </header>
  <div class="filters">
    <span class="info">Filter by:</span><%# @todo DM are these really the filters we're using? %>
    
    <a href="#" class="radio_button filter inactive"><span class="use"></span>Open</a>
    
    <a href="#" class="radio_button filter inactive"><span class="use"></span>Responses</a>
    
    <a href="#" class="radio_button filter inactive"><span class="use"></span>Pending</a>
    
    <a href="#" class="radio_button filter inactive"><span class="use"></span>Recent</a>
    
    <a href="#" class="radio_button filter inactive"><span class="use"></span>Issue</a>
    
    <div class="search"><%# @todo DM full-text search within state? %>
      <input type="text" name="" id="" placeholder="Search state">
      <input type="submit" value="">
    </div>
  </div>

  <%= render partial: 'page' %>
</section>
